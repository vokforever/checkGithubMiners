# Cursor Rules для CapRover

## 🚀 CapRover Deployment

### 📦 Зависимости
- Все зависимости указаны в `requirements.txt`
- **psutil** добавлен для команды `/debug` (мониторинг системы)
- **supabase** для работы с базой данных

### 🔧 Установка в CapRover
```bash
# В CapRover контейнере
pip install -r requirements.txt

# Или отдельно
pip install psutil==5.9.8
```

### 🌐 Переменные окружения
Убедитесь, что в CapRover настроены:
```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
BOT_TOKEN=your_telegram_bot_token
ADMIN_ID=your_admin_telegram_id
CHANNEL_ID=your_channel_id
```

### ⚠️ Важно для Supabase
- Используйте **service_role** key, НЕ **anon** key
- У `service_role` должны быть права на чтение/запись
- Проверьте RLS (Row Level Security) настройки

### 🐳 Docker
Если используете Docker:
```dockerfile
# В Dockerfile добавьте
RUN pip install psutil==5.9.8
```

### 🔍 Отладка
Команда `/debug` покажет:
- Статус системы (память, диск)
- Статус Supabase подключения
- Информацию о файлах данных
- Версию Python и платформу

### 📝 Логи
В CapRover логи доступны через:
- CapRover Dashboard → Apps → Logs
- Или через терминал: `caprover logs --app-name your-app`

### 🚨 Проблемы
1. **psutil не установлен** → `pip install psutil`
2. **Supabase недоступен** → проверьте ключи и права доступа
3. **Бот не запускается** → проверьте переменные окружения
