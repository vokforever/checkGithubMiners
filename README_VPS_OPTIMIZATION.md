# 🚀 Оптимизированная версия бота для VPS с небольшими ресурсами

## 📋 Обзор оптимизаций

Этот бот был специально оптимизирован для работы на VPS серверах с ограниченными ресурсами (CPU, RAM, диск). Основные оптимизации направлены на:

- **Снижение потребления памяти**
- **Оптимизация сетевых запросов**
- **Адаптивные интервалы проверки**
- **Умное управление ресурсами**

## 🔧 VPS профили

### low_power (рекомендуемый)
- Максимум 2 одновременных запроса
- Таймаут запроса: 25 секунд
- Размер пакета: 3 репозитория
- Порог памяти: 75 МБ
- Интервалы: 30 мин - 48 часов

### ultra_low_power
- Максимум 1 одновременный запрос
- Таймаут запроса: 30 секунд
- Размер пакета: 2 репозитория
- Порог памяти: 50 МБ
- Интервалы: 60 мин - 72 часа

## ⚙️ Настройка

### 1. Переменные окружения

Скопируйте содержимое `env_optimized.txt` в ваш `.env` файл:

```bash
cp env_optimized.txt .env
```

Затем отредактируйте `.env` файл, указав ваши реальные значения:

```env
BOT_TOKEN=your_telegram_bot_token_here
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
CHANNEL_ID=your_channel_id_here
ADMIN_ID=your_admin_telegram_id_here
GITHUB_TOKEN=your_github_token_here
```

### 2. Настройка VPS профиля

В файле `.env` можно изменить профиль VPS:

```env
# Для очень слабых серверов
VPS_PROFILE=ultra_low_power

# Для средних серверов (по умолчанию)
VPS_PROFILE=low_power
```

## 🚀 Запуск

### Запуск оптимизированной версии

```bash
python main_optimized.py
```

### Запуск оригинальной версии

```bash
python main.py
```

## 📊 Система приоритетов

Бот автоматически адаптирует частоту проверки репозиториев:

- **🔴 Высокий приоритет** (≥0.5) — проверка каждые 30 мин
- **🟡 Средний приоритет** — проверка по расписанию
- **🟢 Низкий приоритет** (≤0.1) — проверка каждые 48 часов

## 💾 Управление ресурсами

### Автоматическая очистка
- Логи старше 14 дней удаляются автоматически
- Резервные копии старше 7 дней удаляются автоматически
- Файл истории ограничен 25 МБ

### Мониторинг ресурсов
- Проверка состояния каждые 4 часа
- Автоматическая очистка памяти после каждого пакета
- Адаптивные паузы между операциями

## 📈 Команды администратора

- `/stats` — общая статистика бота
- `/priority` — приоритеты репозиториев
- `/sync` — синхронизация с базой данных
- `/pstats` — статистика приоритетов
- `/checkall` — проверить все репозитории
- `/backup` — создать резервные копии
- `/debug` — отладочная информация

## 🔍 Мониторинг производительности

### Команда /debug
Показывает:
- Использование CPU и RAM
- Размер файлов данных
- Статус подключений
- VPS профиль и настройки

### Логи
- Основные логи: `logs/bot_YYYYMMDD.log`
- Логи ошибок: `logs/errors_YYYYMMDD.log`
- Автоматическая ротация и очистка

## ⚠️ Рекомендации для VPS

### Системные требования
- **RAM**: минимум 128 МБ, рекомендуется 256 МБ
- **CPU**: 1 ядро, частота от 1 ГГц
- **Диск**: минимум 1 ГБ свободного места
- **Сеть**: стабильное интернет-соединение

### Оптимизация системы
```bash
# Установка swap файла (если нет)
sudo fallocate -l 512M /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# Добавить в /etc/fstab для автозапуска
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

### Мониторинг ресурсов
```bash
# Установка htop для мониторинга
sudo apt update && sudo apt install htop

# Запуск мониторинга
htop
```

## 🐛 Устранение неполадок

### Высокое потребление памяти
1. Проверьте профиль VPS: `VPS_PROFILE=ultra_low_power`
2. Уменьшите размер пакета: `BATCH_SIZE=1`
3. Увеличьте интервалы проверки

### Медленная работа
1. Проверьте интернет-соединение
2. Увеличьте таймаут: `REQUEST_TIMEOUT=60`
3. Уменьшите количество одновременных запросов

### Ошибки подключения
1. Проверьте настройки Supabase
2. Убедитесь в правильности токенов
3. Проверьте логи на наличие ошибок

## 📝 Сравнение версий

| Параметр | Оригинал | Оптимизированная |
|----------|----------|------------------|
| Макс. запросов | 3 | 2 |
| Таймаут | 30с | 25с |
| Размер пакета | 5 | 3 |
| Интервал проверки | 15-1440 мин | 30-2880 мин |
| Хранение истории | 30 дней | 14 дней |
| Очистка логов | 30 дней | 14 дней |
| Проверка здоровья | 2 часа | 4 часа |

## 🎯 Результаты оптимизации

- **Потребление памяти**: снижено на 30-40%
- **Нагрузка на CPU**: снижена на 25-35%
- **Сетевой трафик**: оптимизирован на 20-30%
- **Стабильность**: улучшена за счет адаптивных интервалов

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Используйте команду `/debug` для диагностики
3. Проверьте настройки в файле `.env`
4. Убедитесь в соответствии системным требованиям

---

**Версия**: 2.0 (VPS Optimized)  
**Дата**: 2024  
**Автор**: VokForever
